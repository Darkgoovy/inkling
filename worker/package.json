{
  "name": "inkling-push-worker",
  "private": true,
  "type": "module",
  "description": "Worker Cloudflare : abonnements Web Push (KV) + envoi des rappels quotidiens (Cron).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "wrangler": "^3.78.0"
  }
}
